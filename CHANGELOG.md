# 更新记录

## 0.5.22

- 播放器更新到 Media3 1.11.0，包含音频缓冲状态及解码器 flush 相关兼容修复。
- 完整流式缓存的播放进度持续不前进时，跨 READY／BUFFERING 状态检测并触发一次旁路重读。
- 保留恢复中的网络退避重试，不因无进度或网络等待超时跳歌；明确文件错误才进入失败跳过流程。
- 扩展 Android 9／API 28 的播放恢复回归，保留 0.5.21 的启动图标。

## 0.5.21

- 启动图标改用 Ocean / Ivory：蓝绿渐变底、暖白 C 形及圆角播放三角。
- 同步多分辨率位图、自适应图标与设计预览。

## 0.5.20

- 修复缓存恢复中的临时网络错误停止重试；继续退避等待，并保留歌曲、队列和位置。
- 删除恢复 45 秒后自动跳歌的判断，不将慢网或断网判为文件损坏。
- 暂停、切歌和熄屏取消待执行的恢复重试；登录、权限及未知错误不自动跳歌。

## 0.5.13

- 音乐来源旁直接提供移除目录入口，确认后清理本机曲库、队列条目和离线缓存，保留原始音乐文件。
- 曲库与队列支持按歌曲名称或目录 / 文件名升降序排序；调整队列时保留当前歌曲、播放进度和播放状态。
- 曲库与队列分别显示实际排序选中态；空队列从当前曲库排序的第一首开始播放。
- 修复部分 Android 7.1 系统在添加目录后显示提示消息时发生的崩溃。
- 排序面板使用单选列表，改善横屏及大字体下的显示和操作。

## 0.5.12

- 横屏播放控制区与左侧当前播放面板对齐，三个主要播放按钮在左栏内居中，不再跨入右侧队列区域。

## 0.5.11

- 横屏播放控制区只保留上一首、播放或暂停、下一首，降低行车场景下误触低频模式控制的风险。
- 随机播放与播放顺序继续通过设置的播放区访问，列表标题保留当前播放顺序提示。

## 0.5.3

- 清理不再被曲库界面、播放队列或离线下载引用的失效曲目元数据；兼容旧版本遗留记录。
- 进度更新后及时移除已不被快照引用的旧队列，继续保留最新及上一份恢复状态。
- 调整 WAL 自动回写与复用策略，回收可释放的日志空间；保留 FULL 同步，不强制等待活跃读写事务。
- 离线索引不可读时暂缓清理；界面刷新期间保留上一份可见曲目，避免影响点击和播放衔接。
- 新增 API 23／33 数据维护回归，覆盖反复扫描、事务回滚、队列恢复、离线引用、大批量清理与日志复用。

## 0.5.2

- 首个 GitHub Release，包含夸克扫码、本地目录、播放恢复、后 3 首自动缓存及熄屏暂停。
- 更新香槟金 C 形唱片与播放符号图标，支持自适应图标与单色主题。
