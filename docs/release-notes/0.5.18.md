本版将随机／顺序切换移到歌曲详情操作行，并精简对应的设置入口。

### 更新内容

- 在“歌词／封面”和“保留离线”之间增加“顺序／随机”按钮，显示当前模式，随机开启时高亮。
- 切换保留当前歌曲、进度和播放意图；空队列时按钮不可用，切换期间防止重复操作。
- 三项操作适配小屏和大字体，状态变化不改变按钮位置；底部主控制行仍只保留上一首、播放／暂停和下一首。
- 移除设置中的随机播放和播放顺序／循环方式入口。此次只调整入口，不改变已有循环行为。
