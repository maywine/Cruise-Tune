新增可选 OneOS 方向盘按键适配，入口为「设置 → 方向盘按键」。

- 播放键、左右键支持单击、双击和长按映射；默认单击对应播放／暂停、上一首、下一首，双击和长按不执行。
- 只有已核验的输入服务与系统 SDK 可以注册监听。按键接收及动作执行前检查媒体菜单，菜单切换、未知状态或查询失败时取消动作。
- 增加按键去重、350ms 双击识别、长按松手保护、断连重连，以及“只检测按键，不控制播放”模式。
- 动作复用现有播放服务，保留暂停、熄屏、音频焦点与完整缓存播放保护；网盘报错后切歌不需要刷新整个目录。
- 补充接口分析、独立实现及测试记录，未包含第三方 APK／DEX／SO、原始内存或账号凭证。

车机专用适配默认关闭，标准媒体按键仍可使用。真实 OneOS 权限、输入服务编号、菜单值及原厂 QQ 音乐是否同时响应，仍需领克 01 实车验证。

验证：337 项 Android 自动化测试通过，release lint 与签名构建通过；BlueStacks 完成覆盖安装、数据保留、映射重启保存、缺少 OneOS 服务、横屏／窄屏及 1.6 倍字体复验。模拟器显示参数已恢复。
