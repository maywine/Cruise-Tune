本版新增播放队列自动跟随当前歌曲。

### 更新内容

- 点播、上一首／下一首和自然切歌时，队列自动定位到当前项；排序使当前项位置变化时也会跟随。
- 当前项已完整可见时不重复滚动；手动拖动和惯性滚动期间延后跟随，播放进度和缓存刷新不会反复抢回位置。
- 点击“队列”可重新定位当前歌曲，长距离定位不播放滚动动画。
- 曲库浏览位置单独保留，不受队列跟随影响；从详情返回时，未切歌则恢复原位置，已切歌则跟随新的当前项。
- 处理异步列表更新与旧回调，避免快速切换列表时滚到过期条目。

队列跟随只改变列表视口，不改变队列顺序、播放状态或曲库数据。
