修复夸克报错后，切换到完整缓存歌曲仍无法播放的问题，并完善缓存重试、登录凭证并发和存储预算处理。0.5.24 的修复合并在本版发布。

- 网盘报错后，上一首／下一首和手动跳转会为选中歌曲重新准备播放；完整缓存歌曲直接读取本地，原本暂停或熄屏时仍保留相应限制。
- 缓存重建遇到临时网络错误会持续退避重试；播放恢复后，后 3 首预缓存可以独立继续。暂停、切歌、熄屏仍会取消旧任务。
- 重新扫码、断开账号和 Cookie 轮换使用同一凭证修订校验，旧响应不能覆盖新登录凭证；取址结果携带对应响应的 Cookie。
- 完整缓存按匹配版本的文件总长度返回结尾，不因缓存长度元数据缺失而多发一次网盘请求；缺失字节仍正常联网读取。
- 缓存预算计入已有缓存占用，避免重启时过度淘汰；空间释放后可自动恢复写入，触及预留空间时继续传递音频并停止可选缓存写入。

回归采用合成音频、真实缓存组件、本地 HTTP 服务和受控播放状态；不代表已完成实车断电、弱网长稳或扬声器音质验收。

仓库附带独立窗口诊断工具和领克底栏调查文档。车机底栏遮挡仍待实车窗口读数，本版尚未修改底栏避让布局；发布 APK 为播放器，不包含独立诊断应用。
