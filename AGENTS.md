# Cruise Tune 工作约定

适用于整个仓库。用户在当前任务中的明确要求优先于本文；已有授权在其范围内持续有效，不重复确认。本文只保留长期开发约束。

## 任务与修改边界

- 开始前确认任务范围、现有改动和完成标准；在授权范围内完成实现与必要验证，不停留在计划或建议。
- 遵循现有 Kotlin、Android Views 和 Media3 结构。修复只做必要修改，不顺手重构、升级依赖或清理无关内容，不覆盖用户的未提交改动。
- 删除代码、配置、字段或已有工作前须有明确授权；静态搜索未发现引用不能单独作为删除依据。
- 不手改生成文件。修改源定义或模板后执行项目生成命令；未经任务明确要求，不提交生成文件或设计、规划文档。
- 只有关键歧义、必要信息缺失或触及授权边界时询问，并先完成不依赖回答的工作。委派不扩大权限；仅在用户明确要求时使用子 agent。

## Git 与发布

- 默认在当前分支工作；需要新分支时说明理由并取得授权。
- 使用 `git commit -s`，不自动添加 AI 的 `Co-Authored-By`。只提交当前任务相关改动。
- `git push` 须有用户明确授权；同一任务已授权的提交与推送直接执行，不逐次确认。未经明确授权，不 reset、amend、rebase 或强推已有提交。
- 创建发布标签、发布 Release 或触发远程构建、测试、lint、生成与验证，须有当前任务的明确授权；文档和脚本不构成授权。
- 应用版本以 `android-app/version.properties` 为唯一来源。发布遵循 [发布说明](docs/releases.md)，保持已发布标签、APK 和签名身份；更正正文使用既有说明同步流程。
- 推送 `docs/release-notes/` 的改动会同步已有 GitHub Release 正文，提交前应检查最终文案。
- 暂存后运行 `python3 tools/check-publish.py` 和 `git diff --cached --check`，检查暂存内容而非仅查看工作目录。

## 验证

- 构建、测试、lint、生成和验证默认在本机执行。本机失败时说明失败命令、原因和缺少的条件，不擅自改用 SSH、远程构建机或 CI。
- 按 [测试指南](android-app/TESTING.md) 选择与改动匹配的检查。先确认生成输入是否齐全，以退出状态和目标用例实际执行为准；全部跳过或没有匹配用例不算通过。
- 测试失败先区分代码、测试逻辑与环境原因，修复本次改动造成的问题及必要阻塞。没有证据时，不将失败归为“与本次修改无关”。
- 最低兼容 API 23，目标车机为 Android 9 / API 28。播放与缓存修改必须覆盖 API 28；原生解码修改另执行 [FLAC JNI 测试](android-app/decoder-flac/README.md#本机验证)，并保持 ARM 32／64 位构建支持。
- 注入播放器错误前等待播放线程确认已有命令；清空主线程队列不等于 seek/play 已完成。模拟器检查与车机音频驱动、扬声器、Dock 和物理按键验证分别报告。
- 仅文档修改检查事实、链接、命令与引用；必要检查通过后不无故扩大或重复测试。未经验证的结果不声称完成。

## 播放与数据

- “已保留”仅表示主动下载的离线音频完整可用，普通流式缓存完成不能标为“已保留”。
- 疑似损坏的流式缓存最多自动旁路重读一次；实际播放位置前进且新副本完整后才替换该曲目缓存，不自动删除已保留的离线音乐。
- 恢复后再次发生明确文件错误才跳到下一首，不改变队列顺序或用户的循环模式；一轮队列均失败后停止。
- 网络、登录、权限错误以及无进度、网络等待超时不作为坏歌跳过的依据。用户暂停、切歌或熄屏取消未完成的恢复。
- 完整缓存的停滞期限按实际播放位置计算，READY／BUFFERING 切换不能重置期限；字节完整不等于可正常播放。
- 缓存身份包含文件版本，凭证响应不得覆盖新登录或恢复已断开的账号；具体行为见 [播放架构](docs/architecture.md) 与 [夸克接入](docs/quark-integration.md)。

## 界面与车机适配

- 横屏主控制行归属左侧当前播放面板，上一首、播放／暂停、下一首铺满可用宽度，不使用固定最大宽度收窄。随机／顺序快捷按钮位于歌词／封面与保留离线之间，不恢复设置中的重复入口。
- 封面保持静止，切歌只作轻微淡入淡出并尊重减少动态效果设置。歌手、专辑加载前后，封面图框位置和尺寸稳定。
- 封面与歌词必须绑定实际播放曲目，不把过渡状态中的旧标签缓存到新曲目；区分尚未加载与确认缺失，同曲目切换封面／歌词复用已读内容。
- 查看封面／歌词时，歌名、进度和主播放按钮保持可用；不使用模态层遮挡。队列跟随只调整视口，不改变队列、播放状态或曲库数据。
- 手动底部留白同时约束窗口背景、弹窗遮罩与触摸区域，不能只移动控件；不控制原车 Dock 显隐。行为见 [界面说明](docs/design.md)。
- OneOS 仅使用已核对的接口与字段布局，不猜测 Binder 事务、厂商方法或权限要求；所有自定义媒体动作受菜单检查约束，注册成功不能代替真实回调。适配细节见 [方向盘说明](docs/steering-wheel.md)。

## 文档与注释

- 项目只提交有长期维护用途的文档：配置与使用方法、当前架构、设计原因、接口约定、测试或排障步骤。同一事实维护一个主要出处，其余链接引用。
- 不提交或保留独立 review 文档。逐轮调查、反编译过程、个人调试经验、测试数量与执行流水账不进入项目文档；有效结论合入现有说明，不改名另建阶段报告。
- 发布说明与更新日志只写该版本新增、调整和修复，使用简短、面向用户的条目；不写测试结果、内部执行过程、未改动功能、通用安装提示或免责声明，遵循 [发布说明规则](docs/releases.md#发布说明写作规则)。
- `android-app/TESTING.md` 只维护测试入口、环境、可复用命令和判定标准，不逐次追加结果，也不为每个用例重复整套步骤。
- 常用文档不堆放参考 APK 的版本、哈希、反编译方法名或取证过程。必要接口约定写入对应技术说明，原始材料留在本机忽略目录。
- 行为变化时直接修正原说明，删除或合并文档时同步修复引用。新的长期项目约束写入根目录 `AGENTS.md`，不写入个人全局配置；文档提交标准见 [文档维护](docs/publishing.md#文档维护)。
- 注释解释原因、隐藏约束和非显然副作用，避免复述代码、记录任务过程或添加无安排的 TODO；行为改变时同步修正相关注释。

## 数据与第三方材料

- 不提交账号凭证、Cookie、Token、客户端实际配置、签名私钥、个人路径、设备标识、真实曲库、数据库、原始日志、含真实数据的截图、APK 或反编译产物。测试使用合成数据，示例配置保持空值。
- 本机配置、工具链和分析材料留在已有忽略目录；不通过修改忽略规则将其纳入提交。第三方许可与归属信息必须保留。
- 使用独立验证包进行设备测试，不用清除正式应用数据解决测试前提；涉及真实账号和设备状态的操作限于用户授权范围。

## 沟通

- 默认使用简洁的简体中文，明确区分事实、推测和未验证事项；发现错误前提时给出可核验依据。
- 长任务提供有实质内容的进度。最终说明改动、原因、验证结果及必要限制，不复述操作流水账；需要用户决定时指出具体缺口或规则来源。
